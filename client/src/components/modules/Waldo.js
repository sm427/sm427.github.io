import React, { Component } from "react";
import { Link } from "@reach/router";

class Waldo extends Component {
    constructor (props) {
        super(props);
    }

    render() {

            // we'll have to render in the photo that we are searching for this component, may want to make another separate component for the next pictuer

    }
}

export default Waldo;